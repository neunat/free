def existanceCheck (inventory,item):
  x = inventory.find(item)

currentInventory
findItemUnedited = input("Which item will you use?")
findItem = findItemUnedited.lower
existanceCheck (currentInventory,findItem)